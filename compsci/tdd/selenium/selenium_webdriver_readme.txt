
selenium + webdrive

selenium ide = click + record a scenario and reply

webdirve = api,  program a test in a programming language


automated testing <=> concurrenty developers reintroduce bugs


automated tests clarify biz specs
tests are exact and measured (not subject to interpretation)

webdrive api {firefox, chrome, ie, htmlunit}

moxilla max version 54 to use with selenium ide
selenium ide = mozilla extension

selenium ide  :  
	record, generate (selenese), play

selenium ide test suite (for less techy users)

exporting testCase 

export  formats  java / junit 4 webdriver

putting breaks in a testcase in ide
base url  in a testcase (sit/uat/..)
ide  options  plugins  e.g. Screnshot on Fail plugin

ch webdriver basics

	